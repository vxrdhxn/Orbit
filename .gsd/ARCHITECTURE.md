# Architecture

> Auto-generated by /map on 2026-06-21

## Overview

Orbit is a VS Code extension providing agentic AI chat features offline, online, and via custom API keys. It allows semantic search, codebase indexing, code reviews, and an integrated chat interface for developers to interact with LLMs without leaving their IDE.

```text
┌─────────────────────────────────────────┐
│        VS Code Extension Host           │
│        (src/extension.ts)               │
├─────────────────────────────────────────┤
│        Business Logic Layer             │
│   (src/services, src/providers)         │
├─────────────────────────────────────────┤
│        Data / LLM Integration Layer     │
│   (src/memory, src/providers)           │
└─────────────────────────────────────────┘
```

## Components

### VS Code Extension Entry Point
- **Purpose:** Initializes the extension, registers commands, and manages the lifecycle of the webview sidebar.
- **Location:** `src/extension.ts`
- **Dependencies:** `vscode`, `src/commands`, `src/ui`

### Chat Provider
- **Purpose:** Handles the interaction between the VS Code extension and the Webview UI, acting as a bridge for user messages and LLM responses.
- **Location:** `src/ChatProvider.ts`
- **Dependencies:** `vscode`, `src/providers/*`, `src/services/*`

### Webview UI
- **Purpose:** The React-based user interface for the Orbit sidebar in VS Code.
- **Location:** `webview-ui/src/App.tsx`
- **Dependencies:** `react`, `@vscode/webview-ui-toolkit`

### LLM Providers
- **Purpose:** Manages the integration with different LLM backends (Online, Local, Local Server, Groq, OpenRouter).
- **Location:** `src/providers/`
- **Dependencies:** Node APIs, external APIs (OpenAI format, etc.)

### Memory & State Management
- **Purpose:** Handles storing and retrieving decision journals, using SQLite for persistence, and synchronizing with the workspace using Git or file-based methods.
- **Location:** `src/memory/`
- **Dependencies:** `better-sqlite3`

### Code Correction & Services
- **Purpose:** Implements background analysis, automated code fixes, and enhanced code reviews.
- **Location:** `src/services/` and `src/correction/`
- **Dependencies:** VS Code APIs, LLM Providers

## Data Flow

1. User interacts with the React UI in the sidebar (`webview-ui/src/App.tsx`).
2. Message is sent via VS Code Webview messaging to the `ChatProvider` (`src/ChatProvider.ts`).
3. The `ChatProvider` orchestrates the request, collecting codebase context (semantic search, indexer) and passing it to the selected LLM Provider (`src/providers/OnlineClient.ts` or similar).
4. The LLM processes the prompt, and the response is streamed back to the React UI.
5. If decisions or code fixes are applied, `DecisionJournal.ts` and `SQLiteMemory.ts` persist the data locally, and `GitJournalSyncService.ts` may sync to `.orbit/journal.json`.

## Integration Points

| Service | Type | Purpose |
|---------|------|---------|
| VS Code API | Extension Host | Native integration with editor, workspace, files, and commands. |
| External LLM APIs | REST API | Fetches completions from Online/Groq/OpenRouter. |
| Local LLM | node-llama-cpp | Runs local models for offline mode. |
| SQLite Database | Local DB | Stores indexing data and AI decision journals. |

## Technical Debt

- [ ] Review implementation of syncing journals via Git vs file to ensure robust concurrent handling.
- [ ] Ensure older dependencies do not present any vulnerabilities (run `npm audit`).

## Conventions

**Naming:** PascalCase for class files and interfaces, camelCase for variables and instances.
**Structure:** Clean separation of concerns with a backend (VS Code Extension) and a frontend (React Webview).
**Testing:** Jest and Mocha tests within the `test/` directory.
